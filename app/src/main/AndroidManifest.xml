<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vie.biddingforcharities" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Global"
        android:allowBackup="true"
        android:icon="@drawable/bfc_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".AccountActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".WatchListActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".BidListActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".AuctionListActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".AuctionFormActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".SellerCategoriesActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".AuctionItemActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".AuctionSearchActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".CharityRequestActivity"
            android:label="@string/app_name" />
    </application>

</manifest>
